name: temporary R0B0E R1 CP1 cleanup

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/r0b0e-r1-cp1-cleanup-20260924.yml
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Delete temporary transfer artifacts
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh api --method DELETE "repos/$GITHUB_REPOSITORY/actions/artifacts/10799205969"
          gh api --method DELETE "repos/$GITHUB_REPOSITORY/actions/artifacts/10798587399"
